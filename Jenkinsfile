pipeline {
    agent any
    stages {
        stage('Git Build') {
            steps { echo '**** Git BUild Start ****' }
        }

        stage('Maven Build') {
            steps {echo '***** Maven BUild with ****'}
        }
    }

}
